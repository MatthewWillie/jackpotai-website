/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://jackpotai.app',  // Replace with your domain
    generateRobotsTxt: true, // Generates robots.txt as well
    sitemapSize: 5000, // Maximum number of URLs per sitemap
  };
  